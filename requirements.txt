# Программа использует только стандартную библиотеку Python.
# Для работы требуется Python версии 3.6 или выше.
# В некоторых дистрибутивах Linux может потребоваться установка пакета python3-tk
# (например, sudo apt-get install python3-tk), но это не относится к pip.
# Установка дополнительных пакетов через pip не требуется.